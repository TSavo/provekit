name = "evm-bytecode-lift"
version = "0.1.0"
protocol_version = "provekit-lift/1"
command = ["./target/release/provekit-lift-evm-bytecode", "--rpc"]
working_dir = "."

[capabilities]
authoring_surfaces = ["evm-bytecode", "evm-assembly", "evm-hex"]
ir_version = "v1.1.0"
emits_signed_mementos = false
