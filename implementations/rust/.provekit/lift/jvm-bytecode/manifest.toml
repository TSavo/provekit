name = "jvm-bytecode-lift"
version = "0.1.0"
protocol_version = "pep/1.7.0"
kind = "lift"
command = ["./target/release/provekit-lift-jvm-bytecode", "--rpc"]
working_dir = "."

[capabilities]
authoring_surfaces = ["jvm-bytecode", "jvm-jasmin"]
ir_version = "v1.1.0"
emits_signed_mementos = false
