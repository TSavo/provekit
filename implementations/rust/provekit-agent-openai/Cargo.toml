[package]
name = "provekit-agent-openai"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "ProvekIt agent backend that drives any OpenAI-compatible chat completions endpoint (Codex, vLLM, LMStudio, Ollama)."

[dependencies]
provekit-agent = { path = "../provekit-agent" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
# Real HTTPS to an OpenAI-compatible endpoint. CI runs without this.
live = []
