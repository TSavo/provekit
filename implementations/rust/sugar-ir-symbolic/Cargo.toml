[package]
name = "sugar-ir-symbolic"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "ProvekIt: Rust kit. Authoring API + IR-JSON serializer for v1.1.0 IR."

[dependencies]
sugar-canonicalizer = { path = "../sugar-canonicalizer" }
sugar-claim-envelope = { path = "../sugar-claim-envelope" }
sugar-proof-envelope = { path = "../sugar-proof-envelope" }
sugar-ir-types = { path = "../sugar-ir-types" }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
sugar-canonicalizer = { path = "../sugar-canonicalizer" }
sugar-claim-envelope = { path = "../sugar-claim-envelope" }
sugar-proof-envelope = { path = "../sugar-proof-envelope" }

[[example]]
name = "parseInt_publish"
path = "../examples/parseInt_publish.rs"

[[example]]
name = "layer2_py_probe"
path = "../examples/layer2_py_probe.rs"
