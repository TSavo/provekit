[package]
name = "sugar-verify-build-rs"
version = "0.1.0"
edition = "2021"
description = "Cargo build.rs integration for provekit verification. The memento IS the verification."
license = "Apache-2.0"
repository = "https://github.com/provekit/provekit"

[dependencies]
