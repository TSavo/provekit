[package]
name = "provekit-agent-claude-code"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "ProvekIt agent backend that drives the Anthropic Claude Code CLI as a subprocess."

[dependencies]
provekit-agent = { path = "../provekit-agent" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
# Real subprocess wiring lives behind a feature flag. CI runs the
# crate with this feature OFF; tests use the in-crate `MockTransport`.
live = []
