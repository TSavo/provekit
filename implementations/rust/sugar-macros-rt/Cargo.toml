[package]
name = "sugar-macros-rt"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "ProvekIt: runtime support for #[provekit::contract] / #[provekit::verify]. Holds the inventory registration types collected at mint time."

[dependencies]
sugar-ir-symbolic = { path = "../sugar-ir-symbolic" }
inventory = { workspace = true }
