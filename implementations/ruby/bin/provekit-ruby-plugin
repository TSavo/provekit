#!/usr/bin/env ruby

$LOAD_PATH.unshift File.expand_path("../lib", __dir__)

require "provekit/pep_server"

unless ARGV.include?("--rpc")
  warn "Usage: provekit-ruby-plugin --rpc"
  exit 1
end

Provekit::PepServer.new.run
