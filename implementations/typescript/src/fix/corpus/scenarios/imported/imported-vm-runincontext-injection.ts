/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-vm-runincontext-injection",
  bugClass: "novel",
  files: {
    ["src/vm-runincontext-injection.js"]: "const vm = require('vm')\n\nexports.handler = async (event) => {\n    var input = event['something']\n    var sandbox = {\n        foo: input\n    }\n    // ruleid: vm-runincontext-injection\n    vm.runInNewContext('safeEval(orderLinesData)', sandbox, { timeout: 2000 })\n\n\n    const code = `\n        var x = ${event['something']};\n    `\n    // ruleid: vm-runincontext-injection\n    vm.runInThisContext(code)\n\n\n    const parsingContext = vm.createContext({name: 'world'})\n    const code1 = `return 'hello ' + '${event['something']}'`\n    // ruleid: vm-runincontext-injection\n    const fn1 = vm.compileFunction(code1, [], { parsingContext })\n\n\n    const context = vm.createContext({name: event['something']})\n    const code2 = `return 'hello ' name`\n    // ruleid: vm-runincontext-injection\n    const fn2 = vm.compileFunction(code2, [], { parsingContext: context })\n\n\n    // ruleid: vm-runincontext-injection\n    const script = new vm.Script(`\n        function add(a, b) {\n          return a + ${event['something']};\n        }\n\n        const x = add(1, 2);\n    `);\n    script.runInThisContext();\n\n\n    // ok: vm-runincontext-injection\n    var sandbox2 = {\n        foo: 1\n    }\n    vm.createContext(sandbox2)\n    vm.runInContext('safeEval(orderLinesData)', sandbox2, { timeout: 2000 })\n\n\n    // ok: vm-runincontext-injection\n    var sandbox3 = {\n        foo: 1\n    }\n    vm.runInNewContext('safeEval(orderLinesData)', sandbox3, { timeout: 2000 })\n\n\n    const code2 = `\n        var x = 1;\n    `\n    // ok: vm-runincontext-injection\n    vm.runInThisContext(code2)\n\n\n    const parsingContext = vm.createContext({name: 'world'})\n    const code3 = `return 'hello ' + name`\n    // ok: vm-runincontext-injection\n    const fn3 = vm.compileFunction(code3, [], { parsingContext })\n\n\n    // ok: vm-runincontext-injection\n    const script1 = new vm.Script(`\n        function add(a, b) {\n          return a + b;\n        }\n\n        const x = add(1, 2);\n    `);\n\n    script1.runInThisContext();\n}",
  },
  bugReport: "Imported from semgrep rule 'vm-runincontext-injection': The `vm` module enables compiling and running code within V8 Virtual Machine contexts. The `vm` module is not a security mechanism. Do not use it to run untrusted code. If code passed to `vm` functions is controlled by user input it could result in command injection. Do not let user input in `vm` functions. metadata: owasp: - A03:2021 - Injection - A05:2025 - Injection cwe: - \"CWE-94: Improper Con (ERROR). Found at src/vm-runincontext-injection.js line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'vm-runincontext-injection': The `vm` module enables compiling and running code within V8 Vir",
        failureDescription: "Imported from semgrep rule 'vm-runincontext-injection': The `vm` module enables compiling and running code within V8 Virtual Machine contexts. The `vm` module is not a security mechanism. Do not use i",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/vm-runincontext-injection.js", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
