/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-apollo-axios-ssrf",
  bugClass: "novel",
  files: {
    ["src/apollo-axios-ssrf.jsx"]: "module.exports = {\n    Query: {\n        requestStatus(parent, args, context, info)\n        {\n            url = args.url\n            const axios = require('axios');\n\n            axios({\n                method: 'GET'\n            })\n            console.log(url)\n            async function getStatus(url) {\n                try {\n                  // ruleid: apollo-axios-ssrf\n                  const response = await axios.request(url);\n                  console.log(response);\n                  var s = response.status;\n                } catch (error) {\n                  console.error(error);\n                  var s = error.code;\n                }\n                return s;\n              }\n            return getStatus(url);\n\n        },\n\n        requestResponse(parent, args, context, info)\n        {\n            url = args.url\n            verb = args.verb\n            payload = args.payload\n            const axios = require('axios');\n            if (payload !== null || payload !== \"\") {\n                axios({\n                    method: verb,\n                    data: payload,\n                    headers: {\n                        'Content-Type': 'application/json'\n                    }\n                })\n            }\n            else {\n                axios({\n                    method: verb\n                })\n            }\n\n            console.log(url)\n            console.log(payload)\n\n            async function getResponse(url) {\n                try {\n                    // ruleid: apollo-axios-ssrf\n                    const response = await axios.request(url);\n                    console.log(response);\n                    var s = response.status;\n                } catch (error) {\n                    console.error(error);\n                    var s = error.message;\n                }\n                return s;\n            }\n\n\n            async function getResponseWithPayload(url, payload) {\n                try {\n                  // ruleid: apollo-axios-ssrf\n                  const response = await axios.request(url);\n                  console.log(response);\n                  var s = response.status;\n                } catch (error) {\n                  console.error(error);\n                  var s = error.message;\n                }\n                return s;\n            }\n\n            if (payload !== null || payload !== \"\") {\n                return getResponse(url);\n            }\n            else {\n                return getResponseWithPayload(url, payload)\n            }\n\n        }\n\n      }\n  };\n",
  },
  bugReport: "Imported from semgrep rule 'apollo-axios-ssrf': User-controllable argument $DATAVAL to $METHOD passed to Axios via internal handler $INNERFUNC. This could be a server-side request forgery. A user could call a restricted API or leak internal headers to an unauthorized party. Validate your user arguments against an allowlist of known URLs, or consider refactoring so that user-controlled data is not necessary. metadata: owasp: - A10:2021 - Server- (WARNING). Found at src/apollo-axios-ssrf.jsx line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'apollo-axios-ssrf': User-controllable argument $DATAVAL to $METHOD passed to Axios via inter",
        failureDescription: "Imported from semgrep rule 'apollo-axios-ssrf': User-controllable argument $DATAVAL to $METHOD passed to Axios via internal handler $INNERFUNC. This could be a server-side request forgery. A user coul",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/apollo-axios-ssrf.jsx", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
