/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-cors-regex-wildcard",
  bugClass: "novel",
  files: {
    ["src/cors-regex-wildcard.tsx"]: "const corsDomains = [\n  /localhost\\:/,\n  /(.+\\.)*foo\\.com$/,\n  /(.+\\.)*foobar\\.com$/, // matches *.foobar.com,\n  // ruleid: cors-regex-wildcard\n  /^(http|https):\\/\\/(qix|qux).biz.baz.foobar.com$/,\n  /^(http|https):\\/\\/www\\.bar\\.com$/,\n  // ruleid: cors-regex-wildcard\n  /^(http|https):\\/\\/www.foo.com$/,\n];\n\nconst CORS = [\n  /localhost\\:/,\n  /(.+\\.)*foo\\.com$/,\n  /(.+\\.)*foobar\\.com$/, // matches *.foobar.com,\n  // ruleid: cors-regex-wildcard\n  /^(http|https):\\/\\/(qix|qux).biz.baz.foobar.com$/,\n  /^(http|https):\\/\\/www\\.bar\\.com$/,\n  // ruleid: cors-regex-wildcard\n  /^(http|https):\\/\\/www.foo.com$/,\n];\n\n// ruleid: cors-regex-wildcard\nconst corsOrigin = /^(http|https):\\/\\/www.foo.com$/;\n\nconst urls = [\n  /localhost\\:/,\n  /(.+\\.)*foo\\.com$/,\n  /(.+\\.)*foobar\\.com$/, // matches *.foobar.com,\n  /^(http|https):\\/\\/(qix|qux).biz.baz.foobar.com$/,\n  /^(http|https):\\/\\/www\\.bar\\.com$/,\n  /^(http|https):\\/\\/www.foo.com$/,\n];\n",
  },
  bugReport: "Imported from semgrep rule 'cors-regex-wildcard': Unescaped '.' character in CORS domain regex $CORS: $PATTERN (WARNING). Found at src/cors-regex-wildcard.tsx line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'cors-regex-wildcard': Unescaped '.' character in CORS domain regex $CORS: $PATTERN (WARNING)",
        failureDescription: "Imported from semgrep rule 'cors-regex-wildcard': Unescaped '.' character in CORS domain regex $CORS: $PATTERN (WARNING). Found at src/cors-regex-wildcard.tsx line 1.",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/cors-regex-wildcard.tsx", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
