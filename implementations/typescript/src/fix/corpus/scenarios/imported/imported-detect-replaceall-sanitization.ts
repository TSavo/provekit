/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-detect-replaceall-sanitization",
  bugClass: "novel",
  files: {
    ["src/detect-replaceall-sanitization.ts"]: "function encodeProductDescription (tableData: any[]) {\n  for (let i = 0; i < tableData.length; i++) {\n    // ruleid: detect-replaceall-sanitization\n    tableData[i].description = tableData[i].description.replaceAll('<', '&lt;').replaceAll('>', '&gt;')\n\n    // ok\n    tableData[i].description = tableData[i].description.replaceAll('<', 'left angle bracket')\n  }\n}\n",
  },
  bugReport: "Imported from semgrep rule 'detect-replaceall-sanitization': Detected a call to `$FUNC()` in an attempt to HTML escape the string `$STR`. Manually sanitizing input through a manually built list can be circumvented in many situations, and it's better to use a well known sanitization library such as `sanitize-html` or `DOMPurify`. metadata: category: security technology: - javascript - typescript owasp: - A07:2017 - Cross-Site Scripting (XSS) - A03:2021 - Inj (INFO). Found at src/detect-replaceall-sanitization.ts line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'detect-replaceall-sanitization': Detected a call to `$FUNC()` in an attempt to HTML escape t",
        failureDescription: "Imported from semgrep rule 'detect-replaceall-sanitization': Detected a call to `$FUNC()` in an attempt to HTML escape the string `$STR`. Manually sanitizing input through a manually built list can be",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/detect-replaceall-sanitization.ts", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
