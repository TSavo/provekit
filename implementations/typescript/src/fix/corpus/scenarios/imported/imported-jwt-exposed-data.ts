/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-jwt-exposed-data",
  bugClass: "novel",
  files: {
    ["src/jwt-exposed-data.js"]: "const jwt = require('jsonwebtoken')\n\nUser.findOne({name: req.body.name}, function(err, user){\n    // ruleid: jwt-exposed-data\n    var token = jwt.sign(user, key, {expiresIn: 60*60*10});\n    res.json({\n        success: true,\n        message: 'Enjoy your token!',\n        token: token\n    });\n});\n\nUser.findOne({name: req.body.name}, function(err, user){\n    // ok: jwt-exposed-data\n    const {name, email} = user\n    var token = jwt.sign({name, email}, key, {expiresIn: 60*60*10});\n    return token;\n});\n\nUser.findOne({name: req.body.name}, function(err, user){\n    // ok: jwt-exposed-data\n    const {name, email} = user\n    var token = jwt.sign({name, email}, key, {expiresIn: 60*60*10});\n    return token;\n});\n",
  },
  bugReport: "Imported from semgrep rule 'jwt-exposed-data': The object is passed strictly to jsonwebtoken.sign(...) Make sure that sensitive information is not exposed through JWT token payload. metadata: owasp: - A02:2017 - Broken Authentication - A04:2021 - Insecure Design - A06:2025 - Insecure Design cwe: - 'CWE-522: Insufficiently Protected Credentials' source-rule-url: https://semgrep.dev/blog/2020/hardcoded-secrets-unverified-tokens-and-other-common- (WARNING). Found at src/jwt-exposed-data.js line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'jwt-exposed-data': The object is passed strictly to jsonwebtoken.sign(...) Make sure that se",
        failureDescription: "Imported from semgrep rule 'jwt-exposed-data': The object is passed strictly to jsonwebtoken.sign(...) Make sure that sensitive information is not exposed through JWT token payload. metadata: owasp: -",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/jwt-exposed-data.js", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
