/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-chrome-remote-interface-compilescript-injection",
  bugClass: "novel",
  files: {
    ["src/chrome-remote-interface-compilescript-injection.js"]: "const CDP = require('chrome-remote-interface');\n\nasync function example(userInput) {\n    let client;\n    try {\n        client = await CDP();\n        const {Runtime} = client;\n        const script1 = \"document.querySelector('p').textContent\"\n        // ok:chrome-remote-interface-compilescript-injection\n        const result = await Runtime.compileScript({expression: script1, sourceURL:\"\", persistScript:false, executionContextId:1});\n        // ruleid:chrome-remote-interface-compilescript-injection\n        const result2 = await Runtime.compileScript({expression: userInput, sourceURL:\"\", persistScript:false, executionContextId:1});\n        // ruleid:chrome-remote-interface-compilescript-injection\n        const result3 = await Runtime.compileScript({expression: 'var x = 123;' + userInput, sourceURL:\"\", persistScript:false, executionContextId:1});\n    } catch (err) {\n        console.error(err);\n    } finally {\n        if (client) {\n            await client.close();\n        }\n    }\n}\n\nasync function example2(userInput) {\n    let client;\n    try {\n        client = await CDP();\n        const {Runtime} = client;\n        const script1 = \"document.querySelector('p').textContent\"\n        // ok:chrome-remote-interface-compilescript-injection\n        const result = await Runtime.evaluate({expression: script1});\n        // ruleid:chrome-remote-interface-compilescript-injection\n        const result2 = await Runtime.evaluate({expression: userInput});\n        // ruleid:chrome-remote-interface-compilescript-injection\n        const result3 = await Runtime.evaluate({expression: 'var x = 123;' + userInput});\n    } catch (err) {\n        console.error(err);\n    } finally {\n        if (client) {\n            await client.close();\n        }\n    }\n}\n\nasync function example3(userInput) {\n    let client;\n    try {\n        client = await CDP();\n        const {Network, Page} = client;\n        Network.requestWillBeSent((params) => {\n            console.log(params.request.url);\n        });\n        await Network.enable();\n        await Page.enable();\n        // ok:chrome-remote-interface-compilescript-injection\n        await Page.navigate({url: 'https://github.com'});\n        // ruleid:chrome-remote-interface-compilescript-injection\n        await Page.navigate({url: userInput});\n        await Page.loadEventFired();\n    } catch (err) {\n        console.error(err);\n    } finally {\n        if (client) {\n            await client.close();\n        }\n    }\n}\n\nfunction example4(userInput) {\n\n    CDP(async (client) => {\n        const {Page} = client;\n        try {\n            await Page.enable();\n            await Page.navigate({url: 'https://github.com'});\n            await Page.loadEventFired();\n            // ok:chrome-remote-interface-compilescript-injection\n            const result = await Page.printToPDF({landscape: true, printBackground: true, headerTemplate: '<h1>Title</h1>'});\n            // ruleid:chrome-remote-interface-compilescript-injection\n            const result2 = await Page.printToPDF({landscape: true, printBackground: true, footerTemplate: userInput});\n            // ruleid:chrome-remote-interface-compilescript-injection\n            const result3 = await Page.printToPDF({landscape: true, printBackground: true, headerTemplate: '<h1>' + userInput + '</h1>'});\n            fs.writeFileSync('page.pdf', Buffer.from(data, 'base64'));\n        } catch (err) {\n            console.error(err);\n        } finally {\n            await client.close();\n        }\n    }).on('error', (err) => {\n        console.error(err);\n    });\n\n}\n\nfunction example5(userInput) {\n    CDP(async (client) => {\n        const {Page} = client;\n        try {\n            const {frameId} = await Page.navigate({url: 'about:blank'});\n            const html = '<html>test</html>';\n            // ok:chrome-remote-interface-compilescript-injection\n            await Page.setDocumentContent({frameId, html});\n            // ruleid:chrome-remote-interface-compilescript-injection\n            await Page.setDocumentContent({frameId, html: userInput});\n        } catch (err) {\n            console.error(err);\n            client.close();\n        }\n    }).on('error', (err) => {\n        console.error(err);\n    });\n}\n",
  },
  bugReport: "Imported from semgrep rule 'chrome-remote-interface-compilescript-injection': If unverified user data can reach the `compileScript` method it can result in Server-Side Request Forgery vulnerabilities metadata: owasp: - A10:2021 - Server-Side Request Forgery (SSRF) - A01:2025 - Broken Access Control cwe: - 'CWE-918: Server-Side Request Forgery (SSRF)' category: security technology: - chrome-remote-interface references: - https://github.com/cyrus-and/chrome-remote-interface c (WARNING). Found at src/chrome-remote-interface-compilescript-injection.js line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'chrome-remote-interface-compilescript-injection': If unverified user data can reach the `com",
        failureDescription: "Imported from semgrep rule 'chrome-remote-interface-compilescript-injection': If unverified user data can reach the `compileScript` method it can result in Server-Side Request Forgery vulnerabilities ",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/chrome-remote-interface-compilescript-injection.js", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
