/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-spawn-shell-true",
  bugClass: "novel",
  files: {
    ["src/spawn-shell-true.js"]: "const {exec, spawnSync} = require('child_process');\n\n// ruleid: spawn-shell-true\nconst ls = spawn('ls', ['-lh', '/usr'], {shell:true});\n\n// ruleid: spawn-shell-true\nconst pid = spawnSync('ls', ['-lh', '/usr'], {shell: '/bin/sh'});\n\n// ok: spawn-shell-true\nspawn('ls', ['-lh', '/usr'], {shell:false});\n\n// ok: spawn-shell-true\nspawn('ls', ['-lh', '/usr'], {});\n",
  },
  bugReport: "Imported from semgrep rule 'spawn-shell-true': Found '$SPAWN' with '{shell: $SHELL}'. This is dangerous because this call will spawn the command using a shell process. Doing so propagates current shell settings and variables, which makes it much easier for a malicious actor to execute commands. Use '{shell: false}' instead. metadata: owasp: - A01:2017 - Injection - A03:2021 - Injection - A05:2025 - Injection cwe: - \"CWE-78: Improper Neutraliza (ERROR). Found at src/spawn-shell-true.js line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'spawn-shell-true': Found '$SPAWN' with '{shell: $SHELL}'. This is dangerous because this cal",
        failureDescription: "Imported from semgrep rule 'spawn-shell-true': Found '$SPAWN' with '{shell: $SHELL}'. This is dangerous because this call will spawn the command using a shell process. Doing so propagates current shel",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/spawn-shell-true.js", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
