/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-nestjs-header-cors-any",
  bugClass: "novel",
  files: {
    ["src/nestjs-header-cors-any.ts"]: "import { Controller, Get, Header } from '@nestjs/common';\nimport { NestFactory } from '@nestjs/core';\nimport { AppService } from './app.service';\nimport { AppModule } from './app.module';\n\nasync function bootstrap1() {\n  // ruleid:nestjs-header-cors-any\n  const app = await NestFactory.create(AppModule, {cors: true});\n  await app.listen(3000);\n}\n\nasync function bootstrap2() {\n  const app = await NestFactory.create(AppModule);\n  // ruleid:nestjs-header-cors-any\n  app.enableCors();\n  await app.listen(3000);\n}\n\nasync function bootstrap3() {\n  // ruleid:nestjs-header-cors-any\n  const app = await NestFactory.create(AppModule, {cors: {origin: '*'}});\n  await app.listen(3000);\n}\n\nasync function bootstrap4() {\n  const app = await NestFactory.create(AppModule);\n  // ruleid:nestjs-header-cors-any\n  app.enableCors({origin: '*'});\n  await app.listen(3000);\n}\n\nasync function bootstrap5() {\n  const app = await NestFactory.create(AppModule);\n  // ok:nestjs-header-cors-any\n  app.enableCors({origin: 'google.com'});\n  await app.listen(3000);\n}\n\n// ruleid:nestjs-header-cors-any\n@Controller()\nexport class AppController1 {\n  constructor(private readonly appService: AppService) {}\n\n  @Get()\n  @Header('Access-Control-Allow-Origin', '*')\n  testCtrl1(): string {\n    return this.appService.getHello();\n  }\n\n}\n\n// ruleid:nestjs-header-cors-any\n@Controller()\nexport class AppController2 {\n  constructor(private readonly appService: AppService) {}\n\n  @Get()\n  @Header('access-control-allow-origin', '*')\n  testCtrl2(): string {\n    return this.appService.getHello();\n  }\n}\n\n// ok:nestjs-header-cors-any\n@Controller()\nexport class AppController3 {\n  constructor(private readonly appService: AppService) {}\n\n  @Get()\n  @Header('Access-Control-Allow-Origin', 'google.com')\n  testCtrlOk(): string {\n    return this.appService.getHello();\n  }\n}\n",
  },
  bugReport: "Imported from semgrep rule 'nestjs-header-cors-any': Access-Control-Allow-Origin response header is set to \"*\". This will disable CORS Same Origin Policy restrictions. metadata: cwe: - 'CWE-183: Permissive List of Allowed Inputs' asvs: section: 'V14: Configuration Verification Requirements' control_id: 14.4.8 Permissive CORS control_url: https://github.com/OWASP/ASVS/blob/master/4.0/en/0x22-V14-Config.md#v144-http-security-headers-requirements versi (WARNING). Found at src/nestjs-header-cors-any.ts line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'nestjs-header-cors-any': Access-Control-Allow-Origin response header is set to \"*\". This wil",
        failureDescription: "Imported from semgrep rule 'nestjs-header-cors-any': Access-Control-Allow-Origin response header is set to \"*\". This will disable CORS Same Origin Policy restrictions. metadata: cwe: - 'CWE-183: Permi",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/nestjs-header-cors-any.ts", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
