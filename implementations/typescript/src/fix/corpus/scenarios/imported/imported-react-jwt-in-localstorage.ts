/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-react-jwt-in-localstorage",
  bugClass: "novel",
  files: {
    ["src/react-jwt-in-localstorage.tsx"]: "import jwt_decode from \"jwt-decode\";\nimport { something } from \"foobar\";\n\nexport const testAuth1 = async (): Promise<void> => {\n  const { token } = await retrieveToken();\n// ruleid: react-jwt-in-localstorage\n  const decoded = jwt_decode<any>(token);\n  localStorage.setItem(TOKEN_PARAM, token);\n};\n\nexport const testAuth2 = async (): Promise<void> => {\n  const { token } = await retrieveToken();\n// ruleid: react-jwt-in-localstorage\n  const decoded = jwt_decode<any>(token);\n  localStorage.setItem(EXPIRES_TOKEN, JSON.stringify(decoded.exp * 1000));\n};\n\nexport const okTestAuth1 = async (): Promise<void> => {\n  const { token } = await retrieveToken();\n// ok: react-jwt-in-localstorage\n  const decoded = jwt_decode<any>(token);\n  something(EXPIRES_TOKEN, JSON.stringify(decoded.exp * 1000));\n};\n",
  },
  bugReport: "Imported from semgrep rule 'react-jwt-in-localstorage': Storing JWT tokens in localStorage known to be a bad practice, consider moving your tokens from localStorage to a HTTP cookie. metadata: cwe: - 'CWE-922: Insecure Storage of Sensitive Information' references: - https://developer.mozilla.org/en-US/docs/Web/HTTP/Cookies category: security owasp: - A01:2021 - Broken Access Control - A01:2025 - Broken Access Control technology: - react subcategory: -  (INFO). Found at src/react-jwt-in-localstorage.tsx line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'react-jwt-in-localstorage': Storing JWT tokens in localStorage known to be a bad practice, c",
        failureDescription: "Imported from semgrep rule 'react-jwt-in-localstorage': Storing JWT tokens in localStorage known to be a bad practice, consider moving your tokens from localStorage to a HTTP cookie. metadata: cwe: - ",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/react-jwt-in-localstorage.tsx", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
