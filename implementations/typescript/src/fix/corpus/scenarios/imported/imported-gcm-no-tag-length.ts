/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-gcm-no-tag-length",
  bugClass: "novel",
  files: {
    ["src/gcm-no-tag-length.js"]: "const crypto = require('node:crypto')\nfunction decrypt1(ciphertext, key) {\n    iv = ciphertext.iv\n    encryptedData = ciphertext.data\n    auth = ciphertext.auth\n\n    algo = \"aes-128-gcm\"\n\n    // ruleid: gcm-no-tag-length\n    const decipher = crypto.createDecipheriv(algo, key, iv);\n    decipher.setAuthTag(auth);\n    let result = decipher.update(encryptedData) + decipher.final();\n\n    return result.toString(\"utf8\");\n}\n\nfunction decrypt2(ciphertext, key) {\n    iv = ciphertext.iv\n    encryptedData = ciphertext.data\n    auth = ciphertext.auth\n    // While this prevents the attack, I'm not quite sure how to capture such length checks and exclude them from the findings\n    assert(auth.length === 16)\n\n    // ruleid: gcm-no-tag-length\n    const decipher = crypto.createDecipheriv(\"aes-192-gcm\", key, iv);\n    decipher.setAuthTag(auth);\n    let result = decipher.update(encryptedData) + decipher.final();\n\n    return result.toString(\"utf8\");\n}\n\nfunction decrypt3(ciphertext, key) {\n    iv = ciphertext.iv\n    encryptedData = ciphertext.data\n    auth = ciphertext.auth\n\n    // ok: gcm-no-tag-length\n    var decipher = crypto.createDecipheriv(\"aes-128-gcm\", key, iv, {authTagLength: 16});\n    decipher.setAuthTag(auth);\n    let result = Buffer.concat([decipher.update(encryptedData), decipher.final()]);\n\n    return result;\n}\n\nfunction decrypt4(ciphertext, key) {\n    iv = ciphertext.iv\n    encryptedData = ciphertext.data\n    auth = ciphertext.auth\n\n    // even though auth tag is shorter than it should be, this looks like a conscious choice\n    // ok: gcm-no-tag-length\n    var decipher = crypto.createDecipheriv(\"aes-256-gcm\", key, iv, {authTagLength: 4});\n    decipher.setAuthTag(auth);\n    let result = Buffer.concat([decipher.update(encryptedData), decipher.final()]);\n\n    return result;\n}\n\nfunction decrypt5(ciphertext, key) {\n    iv = ciphertext.iv\n    encryptedData = ciphertext.data\n    auth = ciphertext.auth\n\n    // hard to capture whether options contain 'authTagLength', so to reduce false positives, just ignore any call with options\n    // ok: gcm-no-tag-length\n    var decipher = crypto.createDecipheriv(\"aes-256-gcm\", key, iv, {someOption: someValue})\n    decipher.setAuthTag(auth)\n    let result = decipher.update(encryptedData)+ decipher.final()\n\n    return result;\n}\n\nfunction decrypt6(ciphertext, key) {\n    iv = ciphertext.iv\n    encryptedData = ciphertext.data\n    auth = ciphertext.auth\n\n    // ok: gcm-no-tag-length\n    var decipher = crypto.createDecipheriv(\"chacha20-poly1305\", key, iv)\n    decipher.setAuthTag(auth)\n    let result = decipher.update(encryptedData)+ decipher.final()\n\n    return result;\n}\n",
  },
  bugReport: "Imported from semgrep rule 'gcm-no-tag-length': The call to 'createDecipheriv' with the Galois Counter Mode (GCM) mode of operation is missing an expected authentication tag length. If the expected authentication tag length is not specified or otherwise checked, the application might be tricked into verifying a shorter-than-expected authentication tag. This can be abused by an attacker to spoof ciphertexts or recover the implicit authentication (ERROR). Found at src/gcm-no-tag-length.js line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'gcm-no-tag-length': The call to 'createDecipheriv' with the Galois Counter Mode (GCM) mode o",
        failureDescription: "Imported from semgrep rule 'gcm-no-tag-length': The call to 'createDecipheriv' with the Galois Counter Mode (GCM) mode of operation is missing an expected authentication tag length. If the expected au",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/gcm-no-tag-length.js", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
