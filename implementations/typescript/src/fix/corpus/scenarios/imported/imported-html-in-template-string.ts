/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-html-in-template-string",
  bugClass: "novel",
  files: {
    ["src/html-in-template-string.js"]: "\n// ok: html-in-template-string\nconst planet = `world`;\nconst greeting = \"hello\";\n\n// ok: html-in-template-string\nlet a = `hello ${planet}`;\n\n// ruleid: html-in-template-string\nlet b = `<h1>hello ${planet}</h1>`;\n\n// ruleid: html-in-template-string\nlet start = `<h1>hello ${planet}`;\n\n// ruleid: html-in-template-string\nlet end = `${planet}</h1><b>foo</b>`;\n\n// ruleid: html-in-template-string\nlet two = `<h1>${greeting} beautiful ${planet}</h1>`;\n\nfunction createHtml() {\n    // from issue #1385\n    // ruleid: html-in-template-string\n    return `<div style=${style.color}>${content}</div>`\n}\n",
  },
  bugReport: "Imported from semgrep rule 'html-in-template-string': This template literal looks like HTML and has interpolated variables. These variables are not HTML-encoded by default. If the variables contain HTML tags, these may be interpreted by the browser, resulting in cross-site scripting (XSS). metadata: cwe: - 'CWE-116: Improper Encoding or Escaping of Output' owasp: - A03:2021 - Injection - A05:2025 - Injection category: security technology: - javascrip (WARNING). Found at src/html-in-template-string.js line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'html-in-template-string': This template literal looks like HTML and has interpolated variabl",
        failureDescription: "Imported from semgrep rule 'html-in-template-string': This template literal looks like HTML and has interpolated variables. These variables are not HTML-encoded by default. If the variables contain HT",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/html-in-template-string.js", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
