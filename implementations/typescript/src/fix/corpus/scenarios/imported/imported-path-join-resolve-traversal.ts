/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-path-join-resolve-traversal",
  bugClass: "novel",
  files: {
    ["src/path-join-resolve-traversal.ts"]: "import { join, resolve } from 'path';\nimport sanitizer from './util/sanitizer';\n\nfunction test1() {\n    function someFunc(entry) {\n        // ruleid:path-join-resolve-traversal\n        var extractPath = join(opts.path, entry.path);\n        return extractFile(extractPath);\n    }\n    someFunc();\n}\n\nfunction test2() {\n    function someFunc(val) {\n        createFile({\n            // ruleid:path-join-resolve-traversal\n            filePath: resolve(opts.path, val)\n        })\n        return true\n    }\n    someFunc()\n}\n\nfunction test3(req,res) {\n    let somePath = req.body.path;\n    // ruleid:path-join-resolve-traversal\n    return join(opts.path, somePath);\n}\n\nfunction test4(req,res) {\n    let data = req.body.path;\n    data.forEach((entry) => {\n    // ruleid:path-join-resolve-traversal\n        var pth = join(opts.path, entry);\n        doSmth(pth);\n    })\n}\n\n\nfunction test5(req,res) {\n    let data = req.body.path;\n    for (let i = 0; i < data.length; i++) {\n        // ruleid:path-join-resolve-traversal\n        var pth = join(opts.path, data[i]);\n        doSmth(pth);\n    }\n}\n\nfunction okTest1(req,res) {\n    let data = ['one', 'two', 'three'];\n    for (let x of data) {\n        // ok:path-join-resolve-traversal\n        var pth = join(opts.path, x);\n        doSmth(pth);\n    }\n}\n\nfunction okTest2() {\n    function someFunc() {\n        createFile({\n            // ok:path-join-resolve-traversal\n            filePath: join(__dirname, 'val')\n        })\n        return true\n    }\n    someFunc()\n}\n\nfunction okTest3(req,res) {\n    let somePath = req.body.path;\n    somePath = somePath.replace(/^(\\.\\.(\\/|\\\\|$))+/, '');\n    // ok:path-join-resolve-traversal\n    return join(opts.path, somePath);\n}\n\nfunction okTest4(req,res) {\n    let somePath = sanitizer(req.body.path);\n    // ok:path-join-resolve-traversal\n    return join(opts.path, somePath);\n}\n\nfunction okTest5(req,res) {\n    let somePath = req.body.path;\n    // ok:path-join-resolve-traversal\n    let result = join(opts.path, somePath);  \n    if (result.indexOf(opts.path) === 0) {\n        return path;\n    }\n    return null\n}\n",
  },
  bugReport: "Imported from semgrep rule 'path-join-resolve-traversal': Detected possible user input going into a `path.join` or `path.resolve` function. This could possibly lead to a path traversal vulnerability, where the attacker can access arbitrary files stored in the file system. Instead, be sure to sanitize or validate user input first. metadata: owasp: - A05:2017 - Broken Access Control - A01:2021 - Broken Access Control - A01:2025 - Broken Access Control cwe: (WARNING). Found at src/path-join-resolve-traversal.ts line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'path-join-resolve-traversal': Detected possible user input going into a `path.join` or `path",
        failureDescription: "Imported from semgrep rule 'path-join-resolve-traversal': Detected possible user input going into a `path.join` or `path.resolve` function. This could possibly lead to a path traversal vulnerability, ",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/path-join-resolve-traversal.ts", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
