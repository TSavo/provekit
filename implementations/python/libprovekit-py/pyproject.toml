[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "libprovekit-py"
version = "0.1.0"
description = "Python libprovekit experiment with honest refusal at the first FFI shim gap."

[tool.setuptools.packages.find]
where = ["."]
