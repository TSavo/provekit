[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "libsugar-py"
version = "0.1.0"
description = "Python libsugar experiment with honest refusal at the first FFI shim gap."

[tool.setuptools.packages.find]
where = ["."]
