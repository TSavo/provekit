.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
build/
# Proof envelopes are mint-time artifacts, not source. The kit's
# attestation lives at ../../.provekit/self-contracts-attestations/swift.json
# (committed); the .proof bytes themselves regenerate on every mint.
*.proof
# Package.resolved is regenerated by SwiftPM on dependency resolution;
# pin via Package.swift's `from:` rule (currently 600.0.0+).
Package.resolved
