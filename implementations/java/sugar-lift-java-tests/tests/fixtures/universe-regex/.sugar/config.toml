# Fixture workspace for Door 3 @Pattern regex-universe tests.
# Vocab is learned from the shared vendored JUnit5 source (one level up);
# the @Pattern regex universe is walked from this workspace's vendor/ dir.
[java-test-assertions]
assertion_source_dirs = ["../vendor/junit5"]
vendor_source_dirs = ["vendor/account", "vendor/jakarta"]
