# Fixture workspace for G1 universe-walk tests.
# Vocab is learned from the shared vendored JUnit5 source (one level up);
# the implementation universe is walked from this workspace's vendor/ dir.
[java-test-assertions]
assertion_source_dirs = ["../vendor/junit5"]
vendor_source_dirs = ["vendor"]
