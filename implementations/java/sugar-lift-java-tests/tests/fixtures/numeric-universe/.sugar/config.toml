# Fixture workspace for G2 numeric-universe-walk tests.
# Vocab is learned from the shared vendored JUnit5 source;
# the numeric universe is walked from this workspace's vendor/jdk21 dir.

[java-test-assertions]
assertion_source_dirs = ["../vendor/junit5"]
vendor_source_dirs = ["vendor/jdk21"]
