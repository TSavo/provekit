name = "cpp-source-lift"
version = "0.1.0-draft"
protocol_version = "provekit-lift/1"
command = ["./target/provekit-lift-cpp-source", "--rpc"]
working_dir = "."

[capabilities]
authoring_surfaces = ["cpp-source"]
ir_version = "v1.1.0"
emits_signed_mementos = false
