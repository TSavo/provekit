# C++ Source Language Signature

Draft C++ source operation algebra emitted by `implementations/cpp/provekit-lift-cpp-source`.
Every operation is namespaced with `cpp:` and carries an explicit `arity_shape` in its op spec.
