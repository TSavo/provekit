[package]
name = "smoke-test-e2e-driver"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Driver for the end-to-end smoke test: lifts the fixture, clusters, names, propagates, compresses, and rewrites."
workspace = "../../../implementations/rust"

[dependencies]
syn = { workspace = true }
proc-macro2 = { workspace = true }
quote = { workspace = true }
provekit-canonicalizer = { path = "../../../implementations/rust/provekit-canonicalizer" }
provekit-ir-symbolic = { path = "../../../implementations/rust/provekit-ir-symbolic" }
provekit-claim-envelope = { path = "../../../implementations/rust/provekit-claim-envelope" }
provekit-proof-envelope = { path = "../../../implementations/rust/provekit-proof-envelope" }
serde = { workspace = true }
serde_json = { workspace = true }

[[bin]]
name = "smoke-test-e2e-driver"
path = "src/main.rs"
