{
  "header": {
    "content": {
      "entries": [
        {
          "concept_name": "concept:rfc8785-jcs-encode-value",
          "emission_template": {
            "kind": "verbatim",
            "template": "match ${param0} {\n        Value::Null => ${param1}.push_str(\"null\"),\n        Value::Bool(true) => ${param1}.push_str(\"true\"),\n        Value::Bool(false) => ${param1}.push_str(\"false\"),\n        Value::Number(n) => ${param1}.push_str(&n.to_string()),\n        Value::String(s) => encode_string(s, ${param1}),\n        Value::Array(items) => {\n            ${param1}.push('[');\n            for (i, item) in items.iter().enumerate() {\n                if i > 0 {\n                    ${param1}.push(',');\n                }\n                encode_value(item, ${param1});\n            }\n            ${param1}.push(']');\n        }\n        Value::Object(map) => {\n            let mut sorted: Vec<(&String, &Value)> = map.iter().collect();\n            sorted.sort_by(|a, b| a.0.cmp(b.0));\n            ${param1}.push('{');\n            for (i, (k, val)) in sorted.iter().enumerate() {\n                if i > 0 {\n                    ${param1}.push(',');\n                }\n                encode_string(k, ${param1});\n                ${param1}.push(':');\n                encode_value(val, ${param1});\n            }\n            ${param1}.push('}');\n        }\n    }"
          },
          "loss_record_contribution": {
            "form": "literal",
            "value": {
              "entries": []
            }
          },
          "signature_guard": {
            "max_params": 2,
            "min_params": 2
          },
          "target_library_tag": "provekit-shim-rfc8785-jcs-rust"
        },
        {
          "concept_name": "concept:rfc8785-jcs-encode-string",
          "emission_template": {
            "kind": "verbatim",
            "template": "${param1}.push('\"');\n    for c in ${param0}.chars() {\n        if c == '\"' {\n            ${param1}.push_str(\"\\\\\\\"\");\n        } else if c == '\\\\' {\n            ${param1}.push_str(\"\\\\\\\\\");\n        } else if (c as u32) < 0x20 {\n            const HEX: &[u8; 16] = b\"0123456789abcdef\";\n            let n = c as u32;\n            ${param1}.push_str(\"\\\\u00\");\n            ${param1}.push(HEX[((n >> 4) & 0xF) as usize] as char);\n            ${param1}.push(HEX[(n & 0xF) as usize] as char);\n        } else {\n            ${param1}.push(c);\n        }\n    }\n    ${param1}.push('\"');"
          },
          "loss_record_contribution": {
            "form": "literal",
            "value": {
              "entries": []
            }
          },
          "signature_guard": {
            "max_params": 2,
            "min_params": 2
          },
          "target_library_tag": "provekit-shim-rfc8785-jcs-rust"
        }
      ],
      "target_language": "rust",
      "template_name": "rust-canonical-bodies-provekit-shim-rfc8785-jcs-rust"
    }
  }
}
