# PHP Language Signature

Draft ProvekIt source-language operation algebra for PHP source lifts.

The catalog covers the `php:` operations emitted by
`implementations/php/provekit-lift-php-source`. It is intentionally an honest
subset: unsupported PHP syntax is refused by the lifter instead of being mapped
to catch-all operations.
