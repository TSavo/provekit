/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-express-ssrf",
  bugClass: "novel",
  files: {
    ["src/express-ssrf.ts"]: "import { Request, Response, NextFunction } from 'express'\n\nconst request = require('request')\n\nmodule.exports = function badNormal () {\n  return (req: Request, res: Response, next: NextFunction) => {\n    const url = \"//\"+req.body.imageUrl\n    const url1 = req.body['imageUrl'] + 123\n    // ruleid: express-ssrf\n    request.get(url)\n    // ruleid: express-ssrf\n    request.get(url1+123)\n    // ok: express-ssrf\n    request.get(`https://reddit.com/${req.query.url}/fooo`)\n    // ok: express-ssrf\n    request.get(\"https://google.com/\"+req.query.url)\n    // ok: express-ssrf\n    request.get(config_value.foo+req.query.url)\n    // ok: express-ssrf\n    request.get(config_value.foo+req.body.shouldalsonotcatch)\n    // ok: express-ssrf\n    request.get(config_value.foo+req)\n\n    // ruleid: express-ssrf\n    request.get(req.body.url)\n    // ruleid: express-ssrf\n    request.get(`${req.query.url}/fooo`)\n    // ruleid: express-ssrf\n    request.get(\"//\"+req.query.url+config_value.url)\n\n    const a = req.body.url\n    // ruleid: express-ssrf\n    request.get(a)\n    // ruleid: express-ssrf\n    request.get(`${url1}/fooo`)\n    // ruleid: express-ssrf\n    request.get(a+config_value.url)\n\n    // ok: express-ssrf\n    request.get(c+a)\n    // ok: express-ssrf\n    request.get(`${c}${a}/fooo`)\n    // ok: express-ssrf\n    request.get(c+a+config_value.url)\n\n    // ok: express-ssrf\n    request.get(c)\n    // ok: express-ssrf\n    request.get(`${c}`)\n    // ok: express-ssrf\n    request.get(c+config_value.url)\n\n    // ruleid: express-ssrf\n    request.get(req.body['url'])\n    // ruleid: express-ssrf\n    request.get(`${req.body['url']}/fooo`)\n    // ruleid: express-ssrf\n    request.get(req.body['url']+config_value.url)\n\n    // ruleid: express-ssrf\n    request.get(\"https://\"+url1)\n    // ruleid: express-ssrf\n    request.get(`https://${req.body['url']}/fooo`)\n    // ruleid: express-ssrf\n    request.get(\"https://\"+req.body['url']+config_value.url)\n    // ruleid: express-ssrf\n    request.get(\"//\"+req.body['url']+config_value.url)\n    // ok: express-ssrf\n    request.get(\"//\"+c+req.body['url']+config_value.url)\n    // todo: express-ssrf\n    request.get(\"https://google.com\"+req.query.url)\n\n}\n}\n\n\nmodule.exports = function badWithTypes () {\n  return ({ body }: Request, res: Response, next: NextFunction) => {\n    const url = body.url\n    // ruleid: express-ssrf\n    request.get(url)\n  }\n\n}\n\nmodule.exports = function goodWithTypes () {\n  return ({ params, query, session }: Request, res: Response, next: NextFunction) => {\n    const url = session\n    // ok: express-ssrf\n     request.get(url)\n  }\n\n}\n\n\nmodule.exports = function advanced () {\n  return ({ body }: Request, res: Response, next: NextFunction) => {\n    const url = body.url\n\n    joinModeOrDeepSemgrep(url, res, next)\n\n  }\n\n  function joinModeOrDeepSemgrep (url: string, res: Response, next: NextFunction) {\n\n      // todo: express-ssrf\n      request.get(url)\n\n  }\n\n}\n",
  },
  bugReport: "Imported from semgrep rule 'express-ssrf': The following request $REQUEST.$METHOD() was found to be crafted from user-input `$REQ` which can lead to Server-Side Request Forgery (SSRF) vulnerabilities. It is recommended where possible to not allow user-input to craft the base request, but to be treated as part of the path or query parameter. When user-input is necessary to craft the request, it is recommeneded to follow OWASP best practices (WARNING). Found at src/express-ssrf.ts line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'express-ssrf': The following request $REQUEST.$METHOD() was found to be crafted from user-in",
        failureDescription: "Imported from semgrep rule 'express-ssrf': The following request $REQUEST.$METHOD() was found to be crafted from user-input `$REQ` which can lead to Server-Side Request Forgery (SSRF) vulnerabilities.",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/express-ssrf.ts", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
