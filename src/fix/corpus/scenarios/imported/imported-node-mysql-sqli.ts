/**
 * IMPORTED FROM semgrep-rules.
 * Auto-generated by src/fix/corpus/import-semgrep.ts.
 * Do not edit by hand: re-run the importer.
 */
import type { CorpusScenario } from "../../scenarios.js";

export const scenario: CorpusScenario = {
  id: "imported-node-mysql-sqli",
  bugClass: "novel",
  files: {
    ["src/node-mysql-sqli.js"]: "var AWS = require('aws-sdk');\nconst mysql = require('mysql2');\n\n\nasync function test2(req,res,next) {\n\n    var createStmt = 'create temporary table ' + req.body.foo + '_jointemp (temp_seq int, '+ a + ' varchar(100)); ';\n  // ok: node-mysql-sqli\n  // diff rule\n  await conn.query(createStmt);\n\n}\nasync function test1(input) {\n  var secretsManager = new AWS.SecretsManager();\n  var secretId = input.arguments[0][2];\n  const secret = await secretsManager.getSecretValue({\n      SecretId: secretId\n      }).promise();\n\n  var secretJson = JSON.parse(secret.SecretString);\n\n  var host = secretJson.host;\n  var user = secretJson.username;\n  var password = secretJson.password;\n\n  let connectionConfig = {\n    host: host,\n    user: user,\n    password: password,\n    connectTimeout: 60000\n  };\n\n  var pool = await mysql.createPool(connectionConfig);\n  var conn = pool.promise();\n\n  var table = input.arguments[0][0];\n  var columnName = input.arguments[0][1];\n\n  var createStmt = 'create temporary table ' + table + '_jointemp (temp_seq int, '+ columnName + ' varchar(100)); ';\n  // ruleid: node-mysql-sqli\n  await conn.query(createStmt);\n\n\n  var values = input.arguments.map((x, i) => \"(\"+i+\",'\"+x[3]+\"')\");\n  var insertStmt = 'insert into ' + table + '_jointemp(temp_seq, '+ columnName +') values ' + values.join(',') + ';';\n  // ruleid: node-mysql-sqli\n  await conn.query({sql: insertStmt, rowsAsArray: true});\n\n  var selectStmt = 'select t2.* FROM ' + table + '_jointemp t1 LEFT OUTER JOIN ' + table + ' t2 using ('+ columnName +') order by temp_seq;'\n  // ruleid: node-mysql-sqli\n  const [results, fields] = await conn.execute(selectStmt);\n\n  // ok: node-mysql-sqli\n  const [results2, fields2] = await conn.execute('SELECT * FROM foobar WHERE id = ?', [columnName]);\n\n  var res = {};\n  if(results.length > 0){\n    res = results.map((row) => JSON.stringify(row));\n    }\n    var response = JSON.stringify({\"results\": res});\n    conn.end();\n  return response;\n};",
  },
  bugReport: "Imported from semgrep rule 'node-mysql-sqli': Detected a `$IMPORT` SQL statement that comes from a function argument. This could lead to SQL injection if the variable is user-controlled and is not properly sanitized. In order to prevent SQL injection, it is recommended to use parameterized queries or prepared statements. metadata: references: - https://www.npmjs.com/package/mysql2 - https://www.npmjs.com/package/mysql - https://cheatsheetseri (WARNING). Found at src/node-mysql-sqli.js line 1.",
  expected: {
    completes: ["intake"],
    outcome: "out_of_scope",
  },
  llmResponses: [
    {
      matchPrompt: "bug-report parser",
      response: JSON.stringify({
        summary: "Imported from semgrep rule 'node-mysql-sqli': Detected a `$IMPORT` SQL statement that comes from a function argument. Th",
        failureDescription: "Imported from semgrep rule 'node-mysql-sqli': Detected a `$IMPORT` SQL statement that comes from a function argument. This could lead to SQL injection if the variable is user-controlled and is not pro",
        fixHint: "imported semgrep rule — out of scope for current principle library",
        codeReferences: [{ file: "src/node-mysql-sqli.js", line: 1 }],
        bugClassHint: "novel",
      }),
    },
    {
      matchPrompt: "classifying a bug report",
      response: JSON.stringify({
        primaryLayer: "out_of_scope",
        secondaryLayers: [],
        artifacts: [],
        rationale: "Imported semgrep rule with no matching principle.",
      }),
    },
  ],
};
