# Provekit configuration for Go callee
# This project is called by C++ via cgo, calls TS via Node-API

[project]
name = "circular-proof-go"
version = "1.0.0"
surface = "go"

[verification]
enabled = true
fail_closed = true

[[bridges]]
name = "finalizeValue"
target = "circular-proof-ts-callee"
target_symbol = "finalizeValue"
source_layer = "go"
target_layer = "typescript"
