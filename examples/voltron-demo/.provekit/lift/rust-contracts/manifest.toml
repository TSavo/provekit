# PEP 1.7.0 `kind = "lift"` plugin manifest for the Rust contracts surface.
# Mirrors examples/provekit-shim-blake3-rust/.provekit/lift/rust-contracts/.
# Dispatches provekit-lift's contracts lane (rust-tests adapter + panic/early-
# return shapes) against this crate's src/ + tests/.
name = "rust-contracts-lift"
command = ["../../implementations/rust/target/debug/provekit-lift", "--rpc"]
working_dir = "."
