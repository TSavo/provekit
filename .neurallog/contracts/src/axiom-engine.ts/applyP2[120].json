{
  "key": "src/axiom-engine.ts/applyP2[120]",
  "file": "/Users/tsavo/Projects/neurallog/src/axiom-engine.ts",
  "function": "applyP2",
  "line": 120,
  "signal_hash": "56aca587ccf20b1194a511e34b7c47d01dfac37c3b777038ef46dafd201250be",
  "proven": [
    {
      "principle": "P6",
      "principle_hash": "c58bc6f165c116c7ffcc8d10caa4b94ce965a05ff0a4b2965f316ee4b5a058be",
      "claim": "PROVEN: extractDeclaredVars guard ensures only contracts with SMT variable declarations",
      "smt2": "; PRINCIPLE: P6\n; LINE: 120\n; PROVEN: extractDeclaredVars guard ensures only contracts with SMT variable declarations\n;         contribute checks — zero-var contracts are silently skipped\n\n(declare-const vars_length Int)\n(declare-const checks_pushed Int)\n\n; Precondition: vars_length is the count returned by extractDeclaredVars\n(assert (>= vars_length 0))\n\n; Transition: if (vars.length === 0) continue  — no push occurs\n(assert (=> (= vars_length 0) (= checks_pushed 0)))\n\n; Transition: if vars_length > 0, exactly one check is pushed per proven property\n(assert (=> (> vars_length 0) (= checks_pushed 1)))\n\n; Negation: a zero-var contract still produces a check\n(assert (= vars_length 0))\n(assert (> checks_pushed 0))\n\n(check-sat)"
    }
  ],
  "violations": [
    {
      "principle": "[NEW]",
      "principle_hash": "",
      "claim": "VIOLATION: earlier.function and later.function are always identical within a group",
      "smt2": "; PRINCIPLE: [NEW]\n; LINE: 120\n; VIOLATION: earlier.function and later.function are always identical within a group\n;            but sourceContract vs targetContract labels suggest different functions —\n;            P2 description \"State mutation between X and Y\" is misleading when X == Y\n\n(declare-const earlier_function Int)   ; hash/id of earlier.function string\n(declare-const later_function Int)     ; hash/id of later.function string\n(declare-const same_group Int)         ; 1 = both contracts are in the same byFunction group\n\n; Transition: grouping key = file + \":\" + function\n; If both are in the same group, they have identical file and function values\n(assert (= same_group 1))\n(assert (=> (= same_group 1) (= earlier_function later_function)))\n\n; Transition: description reads `${earlier.function}:${earlier.line} and ${later.function}:${later.line}`\n; Both function names are the same value — the description implies cross-function which is false\n(declare-const description_implies_different_functions Int)  ; 1 = reader infers different fns\n(assert (= description_implies_different_functions 1))\n\n; Violation: description misleads — functions are identical, not distinct\n(assert (= earlier_function later_function))\n(assert (= description_implies_different_functions 1))\n\n(check-sat)"
    }
  ],
  "depends_on": [],
  "clause_history": [
    {
      "clause": "; PRINCIPLE: P6\n; LINE: 120\n; PROVEN: extractDeclaredVars guard ensures only contracts with SMT variable declarations\n;         contribute checks — zero-var contracts are silently skipped\n\n(declare-const vars_length Int)\n(declare-const checks_pushed Int)\n\n; Precondition: vars_length is the count returned by extractDeclaredVars\n(assert (>= vars_length 0))\n\n; Transition: if (vars.length === 0) continue  — no push occurs\n(assert (=> (= vars_length 0) (= checks_pushed 0)))\n\n; Transition: if vars_length > 0, exactly one check is pushed per proven property\n(assert (=> (> vars_length 0) (= checks_pushed 1)))\n\n; Negation: a zero-var contract still produces a check\n(assert (= vars_length 0))\n(assert (> checks_pushed 0))\n\n(check-sat)",
      "status": "active",
      "weaken_step": 0,
      "witness_count_at_last_weaken": 0,
      "current_witness_count": 0
    },
    {
      "clause": "; PRINCIPLE: [NEW]\n; LINE: 120\n; VIOLATION: earlier.function and later.function are always identical within a group\n;            but sourceContract vs targetContract labels suggest different functions —\n;            P2 description \"State mutation between X and Y\" is misleading when X == Y\n\n(declare-const earlier_function Int)   ; hash/id of earlier.function string\n(declare-const later_function Int)     ; hash/id of later.function string\n(declare-const same_group Int)         ; 1 = both contracts are in the same byFunction group\n\n; Transition: grouping key = file + \":\" + function\n; If both are in the same group, they have identical file and function values\n(assert (= same_group 1))\n(assert (=> (= same_group 1) (= earlier_function later_function)))\n\n; Transition: description reads `${earlier.function}:${earlier.line} and ${later.function}:${later.line}`\n; Both function names are the same value — the description implies cross-function which is false\n(declare-const description_implies_different_functions Int)  ; 1 = reader infers different fns\n(assert (= description_implies_different_functions 1))\n\n; Violation: description misleads — functions are identical, not distinct\n(assert (= earlier_function later_function))\n(assert (= description_implies_different_functions 1))\n\n(check-sat)",
      "status": "active",
      "weaken_step": 0,
      "witness_count_at_last_weaken": 0,
      "current_witness_count": 0
    }
  ]
}