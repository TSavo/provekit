[
  {
    "filePath": "/Users/tsavo/Projects/neurallog/examples/resource-leak.ts",
    "relativePath": "examples/resource-leak.ts",
    "callSites": [
      {
        "line": 8,
        "column": 2,
        "functionName": "processFile",
        "signalText": "console.log(`Processing file: ${path}, fd=${fd}`)",
        "signalType": "log",
        "signalHash": "6ecba451c9f0f2472028ba632c799c785642f5acaa5ead382cdf4c0d1fe12958",
        "functionSource": "function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}",
        "fileSource": "import * as fs from \"fs\";\n\nexport function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}\n\nexport function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}\n\nfunction createConnection(host: string): any {\n  return {\n    execute: (q: string) => ({ rows: [], error: null }),\n    close: () => {},\n  };\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "processFile is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  path: string\nReturn type: string[]\nLocal variables (before this signal):\n  results: string[]",
        "pathConditions": [],
        "callees": [
          "alloc",
          "closeSync",
          "includes",
          "log",
          "openSync",
          "push",
          "readSync",
          "toString",
          "trim"
        ],
        "calledBy": []
      },
      {
        "line": 14,
        "column": 6,
        "functionName": "processFile",
        "signalText": "console.log(`Found error in ${path}: ${line}`)",
        "signalType": "log",
        "signalHash": "b7803c6acd36b7af1ac31511b7bfd119213cb05fa91b26660f841f5a1d31f117",
        "functionSource": "function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}",
        "fileSource": "import * as fs from \"fs\";\n\nexport function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}\n\nexport function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}\n\nfunction createConnection(host: string): any {\n  return {\n    execute: (q: string) => ({ rows: [], error: null }),\n    close: () => {},\n  };\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "processFile is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  path: string\nReturn type: string[]\nLocal variables (before this signal):\n  results: string[]",
        "pathConditions": [
          "(line.includes(\"ERROR\"))"
        ],
        "callees": [
          "alloc",
          "closeSync",
          "includes",
          "log",
          "openSync",
          "push",
          "readSync",
          "toString",
          "trim"
        ],
        "calledBy": []
      },
      {
        "line": 15,
        "column": 23,
        "functionName": "processFile",
        "signalText": "BUG: returns without closing fd",
        "signalType": "comment",
        "signalHash": "d532eca56de925dc3aacda71fef4c9761f8c8c4e60092848be7a9ce3aeddafd0",
        "functionSource": "function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}",
        "fileSource": "import * as fs from \"fs\";\n\nexport function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}\n\nexport function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}\n\nfunction createConnection(host: string): any {\n  return {\n    execute: (q: string) => ({ rows: [], error: null }),\n    close: () => {},\n  };\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "processFile is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  path: string\nReturn type: string[]\nLocal variables (before this signal):\n  results: string[]",
        "pathConditions": [
          "(line.includes(\"ERROR\"))"
        ],
        "callees": [
          "alloc",
          "closeSync",
          "includes",
          "log",
          "openSync",
          "push",
          "readSync",
          "toString",
          "trim"
        ],
        "calledBy": []
      },
      {
        "line": 22,
        "column": 2,
        "functionName": "processFile",
        "signalText": "console.log(`Finished processing ${path}, ${results.length} lines`)",
        "signalType": "log",
        "signalHash": "529aee5fdda943e076b74481f37286f567b323db6b93de2a1cabd027210f0119",
        "functionSource": "function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}",
        "fileSource": "import * as fs from \"fs\";\n\nexport function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}\n\nexport function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}\n\nfunction createConnection(host: string): any {\n  return {\n    execute: (q: string) => ({ rows: [], error: null }),\n    close: () => {},\n  };\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "processFile is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  path: string\nReturn type: string[]\nLocal variables (before this signal):\n  results: string[]",
        "pathConditions": [],
        "callees": [
          "alloc",
          "closeSync",
          "includes",
          "log",
          "openSync",
          "push",
          "readSync",
          "toString",
          "trim"
        ],
        "calledBy": []
      },
      {
        "line": 28,
        "column": 2,
        "functionName": "connectAndQuery",
        "signalText": "console.log(`Connected to ${host}, running query`)",
        "signalType": "log",
        "signalHash": "4c1ad07f8f56ee9544f0a4bfb3b3674dc76c2b5c71016d014803b3c93e03de52",
        "functionSource": "function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}",
        "fileSource": "import * as fs from \"fs\";\n\nexport function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}\n\nexport function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}\n\nfunction createConnection(host: string): any {\n  return {\n    execute: (q: string) => ({ rows: [], error: null }),\n    close: () => {},\n  };\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "connectAndQuery is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  host: string\n  query: string\nReturn type: any",
        "pathConditions": [],
        "callees": [
          "close",
          "createConnection",
          "execute",
          "log"
        ],
        "calledBy": []
      },
      {
        "line": 33,
        "column": 4,
        "functionName": "connectAndQuery",
        "signalText": "console.log(`Query failed: ${result.error}`)",
        "signalType": "log",
        "signalHash": "249ad847c68ecb095b1c734f01dd9748715a059ccf9102d6cc86041cc289a850",
        "functionSource": "function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}",
        "fileSource": "import * as fs from \"fs\";\n\nexport function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}\n\nexport function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}\n\nfunction createConnection(host: string): any {\n  return {\n    execute: (q: string) => ({ rows: [], error: null }),\n    close: () => {},\n  };\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "connectAndQuery is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  host: string\n  query: string\nReturn type: any",
        "pathConditions": [
          "(result.error)"
        ],
        "callees": [
          "close",
          "createConnection",
          "execute",
          "log"
        ],
        "calledBy": []
      },
      {
        "line": 34,
        "column": 18,
        "functionName": "connectAndQuery",
        "signalText": "BUG: connection never closed on error path",
        "signalType": "comment",
        "signalHash": "33c74ff44244e32a9a67ce2068a00fdd580ed04c23edec900d366625cc6751bc",
        "functionSource": "function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}",
        "fileSource": "import * as fs from \"fs\";\n\nexport function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}\n\nexport function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}\n\nfunction createConnection(host: string): any {\n  return {\n    execute: (q: string) => ({ rows: [], error: null }),\n    close: () => {},\n  };\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "connectAndQuery is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  host: string\n  query: string\nReturn type: any",
        "pathConditions": [
          "(result.error)"
        ],
        "callees": [
          "close",
          "createConnection",
          "execute",
          "log"
        ],
        "calledBy": []
      },
      {
        "line": 38,
        "column": 2,
        "functionName": "connectAndQuery",
        "signalText": "console.log(`Query complete, ${result.rows.length} rows`)",
        "signalType": "log",
        "signalHash": "d5efde5f868172d06a79f9812dd428b2f1485ad12c48afc4c22514b562815986",
        "functionSource": "function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}",
        "fileSource": "import * as fs from \"fs\";\n\nexport function processFile(path: string): string[] {\n  const fd = fs.openSync(path, \"r\");\n  const buffer = Buffer.alloc(1024);\n  const results: string[] = [];\n\n  console.log(`Processing file: ${path}, fd=${fd}`);\n\n  let bytesRead = fs.readSync(fd, buffer);\n  while (bytesRead > 0) {\n    const line = buffer.toString(\"utf-8\", 0, bytesRead).trim();\n    if (line.includes(\"ERROR\")) {\n      console.log(`Found error in ${path}: ${line}`);\n      return results;  // BUG: returns without closing fd\n    }\n    results.push(line);\n    bytesRead = fs.readSync(fd, buffer);\n  }\n\n  fs.closeSync(fd);\n  console.log(`Finished processing ${path}, ${results.length} lines`);\n  return results;\n}\n\nexport function connectAndQuery(host: string, query: string): any {\n  const connection = createConnection(host);\n  console.log(`Connected to ${host}, running query`);\n\n  const result = connection.execute(query);\n\n  if (result.error) {\n    console.log(`Query failed: ${result.error}`);\n    return null;  // BUG: connection never closed on error path\n  }\n\n  connection.close();\n  console.log(`Query complete, ${result.rows.length} rows`);\n  return result.rows;\n}\n\nfunction createConnection(host: string): any {\n  return {\n    execute: (q: string) => ({ rows: [], error: null }),\n    close: () => {},\n  };\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "connectAndQuery is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  host: string\n  query: string\nReturn type: any",
        "pathConditions": [
          "!((result.error))"
        ],
        "callees": [
          "close",
          "createConnection",
          "execute",
          "log"
        ],
        "calledBy": []
      }
    ],
    "builtAt": "2026-04-15T02:12:44.541Z"
  }
]