[
  {
    "filePath": "/Users/tsavo/Projects/neurallog/examples/cache.ts",
    "relativePath": "examples/cache.ts",
    "callSites": [
      {
        "line": 18,
        "column": 4,
        "functionName": "cacheGet",
        "logText": "console.log(`Cache miss: ${key}`)",
        "functionSource": "function cacheGet<T>(key: string): T | null {\n  const entry = store.get(key) as CacheEntry<T> | undefined;\n  if (!entry) {\n    console.log(`Cache miss: ${key}`);\n    return null;\n  }\n\n  const age = Date.now() - entry.cachedAt;\n  console.log(`Cache hit: ${key}, age=${age}ms, ttl=${entry.ttlMs}ms`);\n\n  // BUG: returns stale value — checks age but doesn't enforce TTL\n  return entry.value;\n}",
        "fileSource": "/**\n * A caching layer with time-based expiry.\n * Designed to trigger Phase 4: the TTL/staleness pattern\n * is not covered by P1-P7 seed axioms.\n */\n\ninterface CacheEntry<T> {\n  value: T;\n  cachedAt: number;\n  ttlMs: number;\n}\n\nconst store = new Map<string, CacheEntry<unknown>>();\n\nexport function cacheGet<T>(key: string): T | null {\n  const entry = store.get(key) as CacheEntry<T> | undefined;\n  if (!entry) {\n    console.log(`Cache miss: ${key}`);\n    return null;\n  }\n\n  const age = Date.now() - entry.cachedAt;\n  console.log(`Cache hit: ${key}, age=${age}ms, ttl=${entry.ttlMs}ms`);\n\n  // BUG: returns stale value — checks age but doesn't enforce TTL\n  return entry.value;\n}\n\nexport function cacheSet<T>(key: string, value: T, ttlMs: number): void {\n  store.set(key, { value, cachedAt: Date.now(), ttlMs });\n  console.log(`Cache set: ${key}, ttl=${ttlMs}ms`);\n}\n\nexport function getUserProfile(userId: string): { name: string; role: string } {\n  const cached = cacheGet<{ name: string; role: string }>(`user:${userId}`);\n  if (cached) {\n    console.log(`Returning cached profile for ${userId}: role=${cached.role}`);\n    return cached;\n  }\n\n  // Simulate DB fetch\n  const profile = { name: \"User \" + userId, role: \"member\" };\n  cacheSet(`user:${userId}`, profile, 300000); // 5 min TTL\n  console.log(`Fetched and cached profile for ${userId}`);\n  return profile;\n}\n",
        "importSources": [],
        "existingContracts": "(no existing contracts for imports)",
        "callingContext": "cacheGet is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  key: string\nReturn type: T | null",
        "pathConditions": [
          "(!entry)"
        ]
      },
      {
        "line": 23,
        "column": 2,
        "functionName": "cacheGet",
        "logText": "console.log(`Cache hit: ${key}, age=${age}ms, ttl=${entry.ttlMs}ms`)",
        "functionSource": "function cacheGet<T>(key: string): T | null {\n  const entry = store.get(key) as CacheEntry<T> | undefined;\n  if (!entry) {\n    console.log(`Cache miss: ${key}`);\n    return null;\n  }\n\n  const age = Date.now() - entry.cachedAt;\n  console.log(`Cache hit: ${key}, age=${age}ms, ttl=${entry.ttlMs}ms`);\n\n  // BUG: returns stale value — checks age but doesn't enforce TTL\n  return entry.value;\n}",
        "fileSource": "/**\n * A caching layer with time-based expiry.\n * Designed to trigger Phase 4: the TTL/staleness pattern\n * is not covered by P1-P7 seed axioms.\n */\n\ninterface CacheEntry<T> {\n  value: T;\n  cachedAt: number;\n  ttlMs: number;\n}\n\nconst store = new Map<string, CacheEntry<unknown>>();\n\nexport function cacheGet<T>(key: string): T | null {\n  const entry = store.get(key) as CacheEntry<T> | undefined;\n  if (!entry) {\n    console.log(`Cache miss: ${key}`);\n    return null;\n  }\n\n  const age = Date.now() - entry.cachedAt;\n  console.log(`Cache hit: ${key}, age=${age}ms, ttl=${entry.ttlMs}ms`);\n\n  // BUG: returns stale value — checks age but doesn't enforce TTL\n  return entry.value;\n}\n\nexport function cacheSet<T>(key: string, value: T, ttlMs: number): void {\n  store.set(key, { value, cachedAt: Date.now(), ttlMs });\n  console.log(`Cache set: ${key}, ttl=${ttlMs}ms`);\n}\n\nexport function getUserProfile(userId: string): { name: string; role: string } {\n  const cached = cacheGet<{ name: string; role: string }>(`user:${userId}`);\n  if (cached) {\n    console.log(`Returning cached profile for ${userId}: role=${cached.role}`);\n    return cached;\n  }\n\n  // Simulate DB fetch\n  const profile = { name: \"User \" + userId, role: \"member\" };\n  cacheSet(`user:${userId}`, profile, 300000); // 5 min TTL\n  console.log(`Fetched and cached profile for ${userId}`);\n  return profile;\n}\n",
        "importSources": [],
        "existingContracts": "(no existing contracts for imports)",
        "callingContext": "cacheGet is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  key: string\nReturn type: T | null\nLocal variables (before this log statement):\n  age: number",
        "pathConditions": [
          "!((!entry))"
        ]
      },
      {
        "line": 31,
        "column": 2,
        "functionName": "cacheSet",
        "logText": "console.log(`Cache set: ${key}, ttl=${ttlMs}ms`)",
        "functionSource": "function cacheSet<T>(key: string, value: T, ttlMs: number): void {\n  store.set(key, { value, cachedAt: Date.now(), ttlMs });\n  console.log(`Cache set: ${key}, ttl=${ttlMs}ms`);\n}",
        "fileSource": "/**\n * A caching layer with time-based expiry.\n * Designed to trigger Phase 4: the TTL/staleness pattern\n * is not covered by P1-P7 seed axioms.\n */\n\ninterface CacheEntry<T> {\n  value: T;\n  cachedAt: number;\n  ttlMs: number;\n}\n\nconst store = new Map<string, CacheEntry<unknown>>();\n\nexport function cacheGet<T>(key: string): T | null {\n  const entry = store.get(key) as CacheEntry<T> | undefined;\n  if (!entry) {\n    console.log(`Cache miss: ${key}`);\n    return null;\n  }\n\n  const age = Date.now() - entry.cachedAt;\n  console.log(`Cache hit: ${key}, age=${age}ms, ttl=${entry.ttlMs}ms`);\n\n  // BUG: returns stale value — checks age but doesn't enforce TTL\n  return entry.value;\n}\n\nexport function cacheSet<T>(key: string, value: T, ttlMs: number): void {\n  store.set(key, { value, cachedAt: Date.now(), ttlMs });\n  console.log(`Cache set: ${key}, ttl=${ttlMs}ms`);\n}\n\nexport function getUserProfile(userId: string): { name: string; role: string } {\n  const cached = cacheGet<{ name: string; role: string }>(`user:${userId}`);\n  if (cached) {\n    console.log(`Returning cached profile for ${userId}: role=${cached.role}`);\n    return cached;\n  }\n\n  // Simulate DB fetch\n  const profile = { name: \"User \" + userId, role: \"member\" };\n  cacheSet(`user:${userId}`, profile, 300000); // 5 min TTL\n  console.log(`Fetched and cached profile for ${userId}`);\n  return profile;\n}\n",
        "importSources": [],
        "existingContracts": "(no existing contracts for imports)",
        "callingContext": "cacheSet is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  key: string\n  value: T\n  ttlMs: number\nReturn type: void",
        "pathConditions": []
      },
      {
        "line": 37,
        "column": 4,
        "functionName": "getUserProfile",
        "logText": "console.log(`Returning cached profile for ${userId}: role=${cached.role}`)",
        "functionSource": "function getUserProfile(userId: string): { name: string; role: string } {\n  const cached = cacheGet<{ name: string; role: string }>(`user:${userId}`);\n  if (cached) {\n    console.log(`Returning cached profile for ${userId}: role=${cached.role}`);\n    return cached;\n  }\n\n  // Simulate DB fetch\n  const profile = { name: \"User \" + userId, role: \"member\" };\n  cacheSet(`user:${userId}`, profile, 300000); // 5 min TTL\n  console.log(`Fetched and cached profile for ${userId}`);\n  return profile;\n}",
        "fileSource": "/**\n * A caching layer with time-based expiry.\n * Designed to trigger Phase 4: the TTL/staleness pattern\n * is not covered by P1-P7 seed axioms.\n */\n\ninterface CacheEntry<T> {\n  value: T;\n  cachedAt: number;\n  ttlMs: number;\n}\n\nconst store = new Map<string, CacheEntry<unknown>>();\n\nexport function cacheGet<T>(key: string): T | null {\n  const entry = store.get(key) as CacheEntry<T> | undefined;\n  if (!entry) {\n    console.log(`Cache miss: ${key}`);\n    return null;\n  }\n\n  const age = Date.now() - entry.cachedAt;\n  console.log(`Cache hit: ${key}, age=${age}ms, ttl=${entry.ttlMs}ms`);\n\n  // BUG: returns stale value — checks age but doesn't enforce TTL\n  return entry.value;\n}\n\nexport function cacheSet<T>(key: string, value: T, ttlMs: number): void {\n  store.set(key, { value, cachedAt: Date.now(), ttlMs });\n  console.log(`Cache set: ${key}, ttl=${ttlMs}ms`);\n}\n\nexport function getUserProfile(userId: string): { name: string; role: string } {\n  const cached = cacheGet<{ name: string; role: string }>(`user:${userId}`);\n  if (cached) {\n    console.log(`Returning cached profile for ${userId}: role=${cached.role}`);\n    return cached;\n  }\n\n  // Simulate DB fetch\n  const profile = { name: \"User \" + userId, role: \"member\" };\n  cacheSet(`user:${userId}`, profile, 300000); // 5 min TTL\n  console.log(`Fetched and cached profile for ${userId}`);\n  return profile;\n}\n",
        "importSources": [],
        "existingContracts": "(no existing contracts for imports)",
        "callingContext": "getUserProfile is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  userId: string\nReturn type: { name: string; role: string }",
        "pathConditions": [
          "(cached)"
        ]
      },
      {
        "line": 44,
        "column": 2,
        "functionName": "getUserProfile",
        "logText": "console.log(`Fetched and cached profile for ${userId}`)",
        "functionSource": "function getUserProfile(userId: string): { name: string; role: string } {\n  const cached = cacheGet<{ name: string; role: string }>(`user:${userId}`);\n  if (cached) {\n    console.log(`Returning cached profile for ${userId}: role=${cached.role}`);\n    return cached;\n  }\n\n  // Simulate DB fetch\n  const profile = { name: \"User \" + userId, role: \"member\" };\n  cacheSet(`user:${userId}`, profile, 300000); // 5 min TTL\n  console.log(`Fetched and cached profile for ${userId}`);\n  return profile;\n}",
        "fileSource": "/**\n * A caching layer with time-based expiry.\n * Designed to trigger Phase 4: the TTL/staleness pattern\n * is not covered by P1-P7 seed axioms.\n */\n\ninterface CacheEntry<T> {\n  value: T;\n  cachedAt: number;\n  ttlMs: number;\n}\n\nconst store = new Map<string, CacheEntry<unknown>>();\n\nexport function cacheGet<T>(key: string): T | null {\n  const entry = store.get(key) as CacheEntry<T> | undefined;\n  if (!entry) {\n    console.log(`Cache miss: ${key}`);\n    return null;\n  }\n\n  const age = Date.now() - entry.cachedAt;\n  console.log(`Cache hit: ${key}, age=${age}ms, ttl=${entry.ttlMs}ms`);\n\n  // BUG: returns stale value — checks age but doesn't enforce TTL\n  return entry.value;\n}\n\nexport function cacheSet<T>(key: string, value: T, ttlMs: number): void {\n  store.set(key, { value, cachedAt: Date.now(), ttlMs });\n  console.log(`Cache set: ${key}, ttl=${ttlMs}ms`);\n}\n\nexport function getUserProfile(userId: string): { name: string; role: string } {\n  const cached = cacheGet<{ name: string; role: string }>(`user:${userId}`);\n  if (cached) {\n    console.log(`Returning cached profile for ${userId}: role=${cached.role}`);\n    return cached;\n  }\n\n  // Simulate DB fetch\n  const profile = { name: \"User \" + userId, role: \"member\" };\n  cacheSet(`user:${userId}`, profile, 300000); // 5 min TTL\n  console.log(`Fetched and cached profile for ${userId}`);\n  return profile;\n}\n",
        "importSources": [],
        "existingContracts": "(no existing contracts for imports)",
        "callingContext": "getUserProfile is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  userId: string\nReturn type: { name: string; role: string }\nLocal variables (before this log statement):\n  profile: object",
        "pathConditions": [
          "!((cached))"
        ]
      }
    ],
    "builtAt": "2026-04-14T21:03:22.757Z"
  }
]