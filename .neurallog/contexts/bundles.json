[
  {
    "filePath": "/Users/tsavo/Projects/neurallog/examples/state-machine.ts",
    "relativePath": "examples/state-machine.ts",
    "callSites": [
      {
        "line": 21,
        "column": 2,
        "functionName": "submitOrder",
        "signalText": "console.log(`Order ${order.id} submitted with ${order.items.length} items`)",
        "signalType": "log",
        "signalHash": "2b183bf8b31ccddfd04310c262d42dc405c504f0e9a34449f1c11de4314b6adb",
        "functionSource": "function submitOrder(order: Order): void {\n  order.state = \"submitted\";\n  console.log(`Order ${order.id} submitted with ${order.items.length} items`);\n  // BUG: no check that order.state === \"draft\" before transitioning\n}",
        "fileSource": "type OrderState = \"draft\" | \"submitted\" | \"approved\" | \"shipped\" | \"cancelled\";\n\ninterface Order {\n  id: string;\n  state: OrderState;\n  items: { sku: string; qty: number }[];\n  approvedBy: string | null;\n  trackingNumber: string | null;\n}\n\nconst VALID_TRANSITIONS: Record<OrderState, OrderState[]> = {\n  draft: [\"submitted\", \"cancelled\"],\n  submitted: [\"approved\", \"cancelled\"],\n  approved: [\"shipped\", \"cancelled\"],\n  shipped: [],\n  cancelled: [],\n};\n\nexport function submitOrder(order: Order): void {\n  order.state = \"submitted\";\n  console.log(`Order ${order.id} submitted with ${order.items.length} items`);\n  // BUG: no check that order.state === \"draft\" before transitioning\n}\n\nexport function approveOrder(order: Order, approver: string): void {\n  order.approvedBy = approver;\n  order.state = \"approved\";\n  console.log(`Order ${order.id} approved by ${approver}`);\n  // BUG: can approve a cancelled or shipped order\n}\n\nexport function shipOrder(order: Order, tracking: string): void {\n  order.trackingNumber = tracking;\n  order.state = \"shipped\";\n  console.log(`Order ${order.id} shipped, tracking: ${tracking}`);\n  // BUG: can ship a draft order, skipping submit and approve\n}\n\nexport function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "submitOrder is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  order: Order\nReturn type: void",
        "pathConditions": [],
        "callees": [
          "log"
        ],
        "calledBy": []
      },
      {
        "line": 22,
        "column": 2,
        "functionName": "submitOrder",
        "signalText": "BUG: no check that order.state === \"draft\" before transitioning",
        "signalType": "comment",
        "signalHash": "51f3d9ace81000ebe2561d5156f12a622dbab96eddca7e4385cd7c5575163d5a",
        "functionSource": "function submitOrder(order: Order): void {\n  order.state = \"submitted\";\n  console.log(`Order ${order.id} submitted with ${order.items.length} items`);\n  // BUG: no check that order.state === \"draft\" before transitioning\n}",
        "fileSource": "type OrderState = \"draft\" | \"submitted\" | \"approved\" | \"shipped\" | \"cancelled\";\n\ninterface Order {\n  id: string;\n  state: OrderState;\n  items: { sku: string; qty: number }[];\n  approvedBy: string | null;\n  trackingNumber: string | null;\n}\n\nconst VALID_TRANSITIONS: Record<OrderState, OrderState[]> = {\n  draft: [\"submitted\", \"cancelled\"],\n  submitted: [\"approved\", \"cancelled\"],\n  approved: [\"shipped\", \"cancelled\"],\n  shipped: [],\n  cancelled: [],\n};\n\nexport function submitOrder(order: Order): void {\n  order.state = \"submitted\";\n  console.log(`Order ${order.id} submitted with ${order.items.length} items`);\n  // BUG: no check that order.state === \"draft\" before transitioning\n}\n\nexport function approveOrder(order: Order, approver: string): void {\n  order.approvedBy = approver;\n  order.state = \"approved\";\n  console.log(`Order ${order.id} approved by ${approver}`);\n  // BUG: can approve a cancelled or shipped order\n}\n\nexport function shipOrder(order: Order, tracking: string): void {\n  order.trackingNumber = tracking;\n  order.state = \"shipped\";\n  console.log(`Order ${order.id} shipped, tracking: ${tracking}`);\n  // BUG: can ship a draft order, skipping submit and approve\n}\n\nexport function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "submitOrder is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  order: Order\nReturn type: void",
        "pathConditions": [],
        "callees": [
          "log"
        ],
        "calledBy": []
      },
      {
        "line": 28,
        "column": 2,
        "functionName": "approveOrder",
        "signalText": "console.log(`Order ${order.id} approved by ${approver}`)",
        "signalType": "log",
        "signalHash": "dd45e653bc1022afc912b92cda051f32928088d96fc1697081cdf4cc41f71266",
        "functionSource": "function approveOrder(order: Order, approver: string): void {\n  order.approvedBy = approver;\n  order.state = \"approved\";\n  console.log(`Order ${order.id} approved by ${approver}`);\n  // BUG: can approve a cancelled or shipped order\n}",
        "fileSource": "type OrderState = \"draft\" | \"submitted\" | \"approved\" | \"shipped\" | \"cancelled\";\n\ninterface Order {\n  id: string;\n  state: OrderState;\n  items: { sku: string; qty: number }[];\n  approvedBy: string | null;\n  trackingNumber: string | null;\n}\n\nconst VALID_TRANSITIONS: Record<OrderState, OrderState[]> = {\n  draft: [\"submitted\", \"cancelled\"],\n  submitted: [\"approved\", \"cancelled\"],\n  approved: [\"shipped\", \"cancelled\"],\n  shipped: [],\n  cancelled: [],\n};\n\nexport function submitOrder(order: Order): void {\n  order.state = \"submitted\";\n  console.log(`Order ${order.id} submitted with ${order.items.length} items`);\n  // BUG: no check that order.state === \"draft\" before transitioning\n}\n\nexport function approveOrder(order: Order, approver: string): void {\n  order.approvedBy = approver;\n  order.state = \"approved\";\n  console.log(`Order ${order.id} approved by ${approver}`);\n  // BUG: can approve a cancelled or shipped order\n}\n\nexport function shipOrder(order: Order, tracking: string): void {\n  order.trackingNumber = tracking;\n  order.state = \"shipped\";\n  console.log(`Order ${order.id} shipped, tracking: ${tracking}`);\n  // BUG: can ship a draft order, skipping submit and approve\n}\n\nexport function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "approveOrder is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  order: Order\n  approver: string\nReturn type: void",
        "pathConditions": [],
        "callees": [
          "log"
        ],
        "calledBy": []
      },
      {
        "line": 29,
        "column": 2,
        "functionName": "approveOrder",
        "signalText": "BUG: can approve a cancelled or shipped order",
        "signalType": "comment",
        "signalHash": "f83cbdec04a508662e46253805d4147bf1b356d795450facccdda7d8f35fdb1b",
        "functionSource": "function approveOrder(order: Order, approver: string): void {\n  order.approvedBy = approver;\n  order.state = \"approved\";\n  console.log(`Order ${order.id} approved by ${approver}`);\n  // BUG: can approve a cancelled or shipped order\n}",
        "fileSource": "type OrderState = \"draft\" | \"submitted\" | \"approved\" | \"shipped\" | \"cancelled\";\n\ninterface Order {\n  id: string;\n  state: OrderState;\n  items: { sku: string; qty: number }[];\n  approvedBy: string | null;\n  trackingNumber: string | null;\n}\n\nconst VALID_TRANSITIONS: Record<OrderState, OrderState[]> = {\n  draft: [\"submitted\", \"cancelled\"],\n  submitted: [\"approved\", \"cancelled\"],\n  approved: [\"shipped\", \"cancelled\"],\n  shipped: [],\n  cancelled: [],\n};\n\nexport function submitOrder(order: Order): void {\n  order.state = \"submitted\";\n  console.log(`Order ${order.id} submitted with ${order.items.length} items`);\n  // BUG: no check that order.state === \"draft\" before transitioning\n}\n\nexport function approveOrder(order: Order, approver: string): void {\n  order.approvedBy = approver;\n  order.state = \"approved\";\n  console.log(`Order ${order.id} approved by ${approver}`);\n  // BUG: can approve a cancelled or shipped order\n}\n\nexport function shipOrder(order: Order, tracking: string): void {\n  order.trackingNumber = tracking;\n  order.state = \"shipped\";\n  console.log(`Order ${order.id} shipped, tracking: ${tracking}`);\n  // BUG: can ship a draft order, skipping submit and approve\n}\n\nexport function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "approveOrder is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  order: Order\n  approver: string\nReturn type: void",
        "pathConditions": [],
        "callees": [
          "log"
        ],
        "calledBy": []
      },
      {
        "line": 35,
        "column": 2,
        "functionName": "shipOrder",
        "signalText": "console.log(`Order ${order.id} shipped, tracking: ${tracking}`)",
        "signalType": "log",
        "signalHash": "dc69df5adc2d9495d4cde3254f7bf03a4a4465d2a7ab45038b7080b1b2dc48e9",
        "functionSource": "function shipOrder(order: Order, tracking: string): void {\n  order.trackingNumber = tracking;\n  order.state = \"shipped\";\n  console.log(`Order ${order.id} shipped, tracking: ${tracking}`);\n  // BUG: can ship a draft order, skipping submit and approve\n}",
        "fileSource": "type OrderState = \"draft\" | \"submitted\" | \"approved\" | \"shipped\" | \"cancelled\";\n\ninterface Order {\n  id: string;\n  state: OrderState;\n  items: { sku: string; qty: number }[];\n  approvedBy: string | null;\n  trackingNumber: string | null;\n}\n\nconst VALID_TRANSITIONS: Record<OrderState, OrderState[]> = {\n  draft: [\"submitted\", \"cancelled\"],\n  submitted: [\"approved\", \"cancelled\"],\n  approved: [\"shipped\", \"cancelled\"],\n  shipped: [],\n  cancelled: [],\n};\n\nexport function submitOrder(order: Order): void {\n  order.state = \"submitted\";\n  console.log(`Order ${order.id} submitted with ${order.items.length} items`);\n  // BUG: no check that order.state === \"draft\" before transitioning\n}\n\nexport function approveOrder(order: Order, approver: string): void {\n  order.approvedBy = approver;\n  order.state = \"approved\";\n  console.log(`Order ${order.id} approved by ${approver}`);\n  // BUG: can approve a cancelled or shipped order\n}\n\nexport function shipOrder(order: Order, tracking: string): void {\n  order.trackingNumber = tracking;\n  order.state = \"shipped\";\n  console.log(`Order ${order.id} shipped, tracking: ${tracking}`);\n  // BUG: can ship a draft order, skipping submit and approve\n}\n\nexport function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "shipOrder is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  order: Order\n  tracking: string\nReturn type: void",
        "pathConditions": [],
        "callees": [
          "log"
        ],
        "calledBy": []
      },
      {
        "line": 36,
        "column": 2,
        "functionName": "shipOrder",
        "signalText": "BUG: can ship a draft order, skipping submit and approve",
        "signalType": "comment",
        "signalHash": "c8d2c477a40e1f78edd0f88d0537ebc638205c94207ebc5003c2c855aee104de",
        "functionSource": "function shipOrder(order: Order, tracking: string): void {\n  order.trackingNumber = tracking;\n  order.state = \"shipped\";\n  console.log(`Order ${order.id} shipped, tracking: ${tracking}`);\n  // BUG: can ship a draft order, skipping submit and approve\n}",
        "fileSource": "type OrderState = \"draft\" | \"submitted\" | \"approved\" | \"shipped\" | \"cancelled\";\n\ninterface Order {\n  id: string;\n  state: OrderState;\n  items: { sku: string; qty: number }[];\n  approvedBy: string | null;\n  trackingNumber: string | null;\n}\n\nconst VALID_TRANSITIONS: Record<OrderState, OrderState[]> = {\n  draft: [\"submitted\", \"cancelled\"],\n  submitted: [\"approved\", \"cancelled\"],\n  approved: [\"shipped\", \"cancelled\"],\n  shipped: [],\n  cancelled: [],\n};\n\nexport function submitOrder(order: Order): void {\n  order.state = \"submitted\";\n  console.log(`Order ${order.id} submitted with ${order.items.length} items`);\n  // BUG: no check that order.state === \"draft\" before transitioning\n}\n\nexport function approveOrder(order: Order, approver: string): void {\n  order.approvedBy = approver;\n  order.state = \"approved\";\n  console.log(`Order ${order.id} approved by ${approver}`);\n  // BUG: can approve a cancelled or shipped order\n}\n\nexport function shipOrder(order: Order, tracking: string): void {\n  order.trackingNumber = tracking;\n  order.state = \"shipped\";\n  console.log(`Order ${order.id} shipped, tracking: ${tracking}`);\n  // BUG: can ship a draft order, skipping submit and approve\n}\n\nexport function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "shipOrder is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  order: Order\n  tracking: string\nReturn type: void",
        "pathConditions": [],
        "callees": [
          "log"
        ],
        "calledBy": []
      },
      {
        "line": 39,
        "column": 7,
        "functionName": "cancelOrder",
        "signalText": "Function \"cancelOrder\" promises: return value must accurately reflect the capability",
        "signalType": "name:predicate",
        "signalHash": "758dd2c8ed17c5783f9c96edf3742f932af0128708e0db17fc8af6b31cf65c52",
        "functionSource": "function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}",
        "fileSource": "type OrderState = \"draft\" | \"submitted\" | \"approved\" | \"shipped\" | \"cancelled\";\n\ninterface Order {\n  id: string;\n  state: OrderState;\n  items: { sku: string; qty: number }[];\n  approvedBy: string | null;\n  trackingNumber: string | null;\n}\n\nconst VALID_TRANSITIONS: Record<OrderState, OrderState[]> = {\n  draft: [\"submitted\", \"cancelled\"],\n  submitted: [\"approved\", \"cancelled\"],\n  approved: [\"shipped\", \"cancelled\"],\n  shipped: [],\n  cancelled: [],\n};\n\nexport function submitOrder(order: Order): void {\n  order.state = \"submitted\";\n  console.log(`Order ${order.id} submitted with ${order.items.length} items`);\n  // BUG: no check that order.state === \"draft\" before transitioning\n}\n\nexport function approveOrder(order: Order, approver: string): void {\n  order.approvedBy = approver;\n  order.state = \"approved\";\n  console.log(`Order ${order.id} approved by ${approver}`);\n  // BUG: can approve a cancelled or shipped order\n}\n\nexport function shipOrder(order: Order, tracking: string): void {\n  order.trackingNumber = tracking;\n  order.state = \"shipped\";\n  console.log(`Order ${order.id} shipped, tracking: ${tracking}`);\n  // BUG: can ship a draft order, skipping submit and approve\n}\n\nexport function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "cancelOrder is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  order: Order\n  reason: string\nReturn type: void",
        "pathConditions": [],
        "callees": [],
        "calledBy": []
      },
      {
        "line": 40,
        "column": 2,
        "functionName": "cancelOrder",
        "signalText": "console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`)",
        "signalType": "log",
        "signalHash": "80e1ffa550aafa79ecf48370782f8f07da6b0b5e33e84eeaa1e227862aa68179",
        "functionSource": "function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}",
        "fileSource": "type OrderState = \"draft\" | \"submitted\" | \"approved\" | \"shipped\" | \"cancelled\";\n\ninterface Order {\n  id: string;\n  state: OrderState;\n  items: { sku: string; qty: number }[];\n  approvedBy: string | null;\n  trackingNumber: string | null;\n}\n\nconst VALID_TRANSITIONS: Record<OrderState, OrderState[]> = {\n  draft: [\"submitted\", \"cancelled\"],\n  submitted: [\"approved\", \"cancelled\"],\n  approved: [\"shipped\", \"cancelled\"],\n  shipped: [],\n  cancelled: [],\n};\n\nexport function submitOrder(order: Order): void {\n  order.state = \"submitted\";\n  console.log(`Order ${order.id} submitted with ${order.items.length} items`);\n  // BUG: no check that order.state === \"draft\" before transitioning\n}\n\nexport function approveOrder(order: Order, approver: string): void {\n  order.approvedBy = approver;\n  order.state = \"approved\";\n  console.log(`Order ${order.id} approved by ${approver}`);\n  // BUG: can approve a cancelled or shipped order\n}\n\nexport function shipOrder(order: Order, tracking: string): void {\n  order.trackingNumber = tracking;\n  order.state = \"shipped\";\n  console.log(`Order ${order.id} shipped, tracking: ${tracking}`);\n  // BUG: can ship a draft order, skipping submit and approve\n}\n\nexport function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "cancelOrder is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  order: Order\n  reason: string\nReturn type: void",
        "pathConditions": [],
        "callees": [
          "log"
        ],
        "calledBy": []
      },
      {
        "line": 42,
        "column": 2,
        "functionName": "cancelOrder",
        "signalText": "BUG: can cancel a shipped order",
        "signalType": "comment",
        "signalHash": "994611c7c4e23e0a7f91902f328e735851809346221acca51be23ba26e7b0a81",
        "functionSource": "function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}",
        "fileSource": "type OrderState = \"draft\" | \"submitted\" | \"approved\" | \"shipped\" | \"cancelled\";\n\ninterface Order {\n  id: string;\n  state: OrderState;\n  items: { sku: string; qty: number }[];\n  approvedBy: string | null;\n  trackingNumber: string | null;\n}\n\nconst VALID_TRANSITIONS: Record<OrderState, OrderState[]> = {\n  draft: [\"submitted\", \"cancelled\"],\n  submitted: [\"approved\", \"cancelled\"],\n  approved: [\"shipped\", \"cancelled\"],\n  shipped: [],\n  cancelled: [],\n};\n\nexport function submitOrder(order: Order): void {\n  order.state = \"submitted\";\n  console.log(`Order ${order.id} submitted with ${order.items.length} items`);\n  // BUG: no check that order.state === \"draft\" before transitioning\n}\n\nexport function approveOrder(order: Order, approver: string): void {\n  order.approvedBy = approver;\n  order.state = \"approved\";\n  console.log(`Order ${order.id} approved by ${approver}`);\n  // BUG: can approve a cancelled or shipped order\n}\n\nexport function shipOrder(order: Order, tracking: string): void {\n  order.trackingNumber = tracking;\n  order.state = \"shipped\";\n  console.log(`Order ${order.id} shipped, tracking: ${tracking}`);\n  // BUG: can ship a draft order, skipping submit and approve\n}\n\nexport function cancelOrder(order: Order, reason: string): void {\n  console.log(`Order ${order.id} cancelled: ${reason}, was in state ${order.state}`);\n  order.state = \"cancelled\";\n  // BUG: can cancel a shipped order\n}\n",
        "importSources": [],
        "existingContracts": "(dependency contracts injected at derivation time)",
        "callingContext": "cancelOrder is module-private. Only called within this module.",
        "typeContext": "Parameters:\n  order: Order\n  reason: string\nReturn type: void",
        "pathConditions": [],
        "callees": [
          "log"
        ],
        "calledBy": []
      }
    ],
    "builtAt": "2026-04-15T02:28:12.745Z"
  }
]